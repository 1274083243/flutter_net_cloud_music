import 'package:flutter/material.dart';

/// 未知错误的显示页面
class UnKnowPage extends StatelessWidget {
  const UnKnowPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("页面走丢了！！"));
  }
}
