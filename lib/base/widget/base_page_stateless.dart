import 'package:flutter/material.dart';

class BasePageStateless extends StatelessWidget {
  const BasePageStateless({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
